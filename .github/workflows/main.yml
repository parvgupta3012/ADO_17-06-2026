name: basic_pipeline
on: 
  workflow_dispatch
  
jobs: 
  build:
    runs-on: ubuntu-latest

    steps:
      - name: code checkout
        uses: actions/checkout@v4

      - name: print message
        run: echo "hello..how r u ?" 
